<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_password_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1C1C1C"
    android:padding="16dp">

    <!-- Título -->
    <TextView
        android:id="@+id/tvTituloSenhas"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Senhas"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:background="#F1F1F1"
        android:padding="12dp" />

    <!-- Lista de senhas -->
    <ScrollView
        android:id="@+id/passwordScrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tvTituloSenhas"
        android:layout_above="@+id/fabAdd">

        <LinearLayout
            android:id="@+id/containerSenhas"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="16dp"
            android:layout_marginBottom="80dp" />
    </ScrollView>

    <!-- Botão flutuante -->
    <Button
        android:id="@+id/fabAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="＋ CREATE"
        android:backgroundTint="#7F00FF"
        android:textColor="#FFFFFF"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:drawableLeft="@drawable/ic_add"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:elevation="8dp" />

</RelativeLayout>
